# Deploy this agency as its own Railway service (not Asymmetry).
# Connect the GitHub repo in the Railway dashboard — no GitHub PAT required.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
